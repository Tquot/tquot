"use client";

/**
 * Re-export del HotelCard visual (canvas) manteniendo el path histórico
 * `components/quote-canvas/HotelCard` para no romper imports existentes.
 */
export { HotelCard } from "@/components/canvas/HotelCard";
